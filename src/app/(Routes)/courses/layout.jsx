


export const metadata = {
  title: {
    default: "Courses",
    template: "Courses | %s",
  },
};

const CoursesLayout =  ({ children, params }) => {
  
  return (
    <>
      {children}
    </>
  );
};

export default CoursesLayout;
